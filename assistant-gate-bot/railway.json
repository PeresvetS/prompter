{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo '🔧 Starting build process...' && npx prisma generate && npm run build && echo '✅ Build completed successfully' && echo '📁 Checking admin panel files:' && ls -la public/admin/ || echo '⚠️ Admin files not found'",
    "watchPatterns": ["**/*.ts", "**/*.tsx", "**/*.js", "**/*.jsx", "**/*.json"]
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "npm run deploy",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "healthcheckInterval": 30
  }
}

{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npx prisma generate && npm run build && echo 'Build completed' && ls -la public/admin/ || echo 'Admin files not found'"
  },
  "deploy": {
    "startCommand": "npm run start:railway",
    "restartPolicyType": "ON_FAILURE"
  }
}
